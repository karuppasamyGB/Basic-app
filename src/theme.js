// theme.js
import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    customPurple: "#7928CA", // Ensure this color is properly defined
  },
});

export default theme;
